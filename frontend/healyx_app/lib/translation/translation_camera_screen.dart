/*
import 'package:flutter/material.dart';
import 'translation_upload_screen.dart';

class TranslationCameraScreen extends StatelessWidget {
  const TranslationCameraScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('카메라 번역'),
      ),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            Navigator.push(
              context,
              MaterialPageRoute(
                builder: (context) => const TranslationUploadScreen(),
              ),
            );
          },
          child: const Text('업로드 화면으로 이동'),
        ),
      ),
    );
  }
}
*/